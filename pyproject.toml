[project]
name = "activities-publish"
version = "0.1.0"
description = "加拿大活动自动发布系统 — 抓取 + AI 处理 + API 发布"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "anthropic>=0.40",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "apscheduler>=3.10",
    "loguru>=0.7",
    "asyncpg>=0.30",
    "playwright>=1.40",
    "bleach>=6.0",
    "fastapi>=0.110",
    "uvicorn>=0.27",
    "jinja2>=3.1",
    "python-multipart>=0.0.6",
]
